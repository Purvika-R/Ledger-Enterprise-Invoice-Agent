import { CheckCircle2, CircleDashed, ShieldAlert } from "lucide-react";
import { motion } from "framer-motion";

type FieldValidationResult = { valid: boolean; message: string };
type Props = { fieldValidation: Record<string, FieldValidationResult> | null; approved: boolean; onApprove: () => void };
export default function ApprovalPanel({ fieldValidation, approved, onApprove }: Props) { if (!fieldValidation) return null; const outstanding = Object.values(fieldValidation).filter((result) => !result.valid); const allValid = outstanding.length === 0; if (approved) return <motion.section initial={{ opacity: 0, scale: .98 }} animate={{ opacity: 1, scale: 1 }} className="rounded-3xl border border-emerald-200 bg-emerald-50 p-6 text-emerald-800"><div className="flex items-center gap-3"><CheckCircle2 size={24} /><div><h2 className="font-semibold">Invoice approved</h2><p className="text-sm">The reviewed invoice is ready for the next workflow step.</p></div></div></motion.section>; return <section className="premium-card rounded-3xl p-5"><div className="flex items-start justify-between gap-4"><div><p className="text-xs font-bold uppercase tracking-[0.14em] text-slate-400">Final authority</p><h2 className="mt-1 text-lg font-semibold text-slate-900">Human review</h2><p className="mt-1 text-sm text-slate-500">{allValid ? "All checks are complete. You can approve this invoice." : `${outstanding.length} field${outstanding.length === 1 ? "" : "s"} still require attention before approval.`}</p></div>{allValid ? <CheckCircle2 className="text-emerald-500" /> : <ShieldAlert className="text-amber-500" />}</div><div className="mt-5"><button onClick={onApprove} disabled={!allValid} className="rounded-xl bg-gradient-to-r from-blue-600 to-indigo-600 px-5 py-3 text-sm font-semibold text-white shadow-lg shadow-indigo-500/20 transition hover:brightness-105 disabled:cursor-not-allowed disabled:opacity-40">Approve invoice</button></div><div className="mt-4 flex items-center gap-2 text-xs text-slate-500"><CircleDashed size={14} />Approval remains a human-controlled workflow.</div></section>; }
