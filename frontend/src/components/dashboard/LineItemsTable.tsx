import { ListChecks } from "lucide-react";

type FieldValidationResult = { valid: boolean; message: string };
type Props = { items: any[]; fieldValidation?: Record<string, FieldValidationResult>; liveFieldValidation?: Record<string, FieldValidationResult>; onItemChange?: (index: number, field: string, value: string) => void; locked?: boolean };
export default function LineItemsTable({ items, fieldValidation, liveFieldValidation, onItemChange, locked }: Props) { const originalIssue = fieldValidation?.line_items; const editable = Boolean(originalIssue && !originalIssue.valid) && !locked; return <section className="premium-card overflow-hidden rounded-3xl"><div className="flex items-center justify-between p-5"><div><p className="text-xs font-bold uppercase tracking-[0.14em] text-slate-400">Extracted table</p><h2 className="mt-1 text-lg font-semibold text-slate-900">Invoice line items</h2></div><ListChecks className="text-indigo-600" size={21} /></div>{originalIssue && !originalIssue.valid && <div className={`mx-5 mb-4 rounded-xl border p-3 text-sm ${liveFieldValidation?.line_items?.valid ? "border-emerald-200 bg-emerald-50 text-emerald-700" : "border-rose-200 bg-rose-50 text-rose-700"}`}>{liveFieldValidation?.line_items?.valid ? "Correction complete." : liveFieldValidation?.line_items?.message || originalIssue.message}</div>}<div className="overflow-x-auto"><table className="min-w-full text-sm"><thead className="sticky top-0 bg-slate-50 text-left text-xs font-bold uppercase tracking-[0.1em] text-slate-500"><tr><th className="px-5 py-3">Description</th><th className="px-5 py-3 text-center">Qty</th><th className="px-5 py-3 text-right">Unit price</th><th className="px-5 py-3 text-right">Amount</th></tr></thead><tbody className="divide-y divide-slate-100">{items.map((item, index) => <tr key={index} className="transition hover:bg-indigo-50/40 even:bg-slate-50/40"><td className="px-5 py-4 font-medium text-slate-700">{item.description || "—"}</td><td className="px-5 py-4 text-center text-slate-600">{item.quantity ?? "—"}</td><td className="px-5 py-4 text-right text-slate-600">{item.unit_price ?? "—"}</td><td className="px-5 py-4 text-right font-semibold text-slate-800">{editable ? <input type="number" value={item.amount ?? ""} onChange={(event) => onItemChange?.(index, "amount", event.target.value)} className="w-28 rounded-lg border border-rose-200 bg-white px-2 py-1 text-right outline-none focus:border-indigo-400" /> : item.amount ?? "—"}</td></tr>)}{!items.length && <tr><td colSpan={4} className="px-5 py-10 text-center text-slate-500">No line items were extracted.</td></tr>}</tbody></table></div></section>; }
