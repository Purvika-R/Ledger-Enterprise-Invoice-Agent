import { CheckCircle2, ChevronDown, CircleAlert, ShieldCheck } from "lucide-react";

type FieldValidation = { valid: boolean; message: string };
type Props = { validationErrors: string[]; fieldValidation?: Record<string, FieldValidation> };
export default function ValidationCard({ validationErrors, fieldValidation }: Props) { const errors = validationErrors ?? []; const passed = errors.length === 0; return <section className="premium-card rounded-3xl p-5"><div className="mb-5 flex items-center justify-between"><div><p className="text-xs font-bold uppercase tracking-[0.14em] text-slate-400">Integrity check</p><h2 className="mt-1 text-lg font-semibold text-slate-900">Validation</h2></div><ShieldCheck className={passed ? "text-emerald-500" : "text-amber-500"} size={22} /></div><div className={`rounded-2xl border p-3.5 ${passed ? "border-emerald-100 bg-emerald-50 text-emerald-800" : "border-amber-100 bg-amber-50 text-amber-800"}`}><div className="flex items-center gap-2 font-semibold">{passed ? <CheckCircle2 size={18} /> : <CircleAlert size={18} />}{passed ? "Validation passed" : `${errors.length} issue${errors.length === 1 ? "" : "s"} require review`}</div>{!passed && <ul className="mt-2 space-y-1.5 text-sm leading-5">{errors.map((error) => <li key={error}>• {error}</li>)}</ul>}</div>{fieldValidation && <details className="group mt-4"><summary className="flex cursor-pointer list-none items-center justify-between text-xs font-semibold uppercase tracking-[0.12em] text-slate-500">Field checks <ChevronDown size={15} className="transition group-open:rotate-180" /></summary><div className="mt-3 space-y-2">{Object.entries(fieldValidation).map(([field, result]) => <div key={field} className="flex items-center justify-between rounded-xl bg-slate-50 px-3 py-2 text-xs"><span className="capitalize text-slate-600">{field.replace(/_/g, " ")}</span><span className={result.valid ? "font-semibold text-emerald-600" : "font-semibold text-rose-600"}>{result.valid ? "Valid" : "Needs review"}</span></div>)}</div></details>}</section>; }
