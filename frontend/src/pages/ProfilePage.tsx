import { motion } from "framer-motion";
import { BadgeCheck, Mail, ShieldCheck, UserRound } from "lucide-react";

import { useAuth } from "../context/AuthContext";

export default function ProfilePage() { const { user } = useAuth(); const initials = user?.name?.split(" ").map((part) => part[0]).join("").slice(0, 2).toUpperCase(); return <main className="mx-auto max-w-5xl px-4 py-7 sm:px-7"><motion.section initial={{ opacity: 0, y: 10 }} animate={{ opacity: 1, y: 0 }} className="overflow-hidden rounded-3xl border border-indigo-100 bg-gradient-to-br from-white via-white to-indigo-50 p-6 shadow-[0_18px_50px_-32px_rgba(30,41,59,0.45)] sm:p-8"><div className="flex flex-col gap-5 sm:flex-row sm:items-center"><div className="grid h-18 w-18 place-items-center rounded-3xl bg-gradient-to-br from-blue-500 to-indigo-600 text-xl font-bold text-white shadow-lg shadow-indigo-500/25">{initials || <UserRound />}</div><div><p className="text-xs font-bold uppercase tracking-[0.16em] text-indigo-600">Account</p><h1 className="mt-1 text-3xl font-semibold tracking-tight text-slate-950">{user?.name}</h1><p className="mt-1 text-sm text-slate-500">Your Ledger workspace profile and access details.</p></div><div className="sm:ml-auto"><span className="inline-flex rounded-full bg-indigo-100 px-3 py-1.5 text-xs font-bold capitalize text-indigo-700">{user?.role}</span></div></div><div className="mt-8 grid gap-4 sm:grid-cols-3"><div className="rounded-2xl border border-slate-100 bg-white/80 p-5"><Mail className="text-blue-600" size={20} /><p className="mt-4 text-xs font-medium text-slate-500">Email address</p><p className="mt-1 break-all font-semibold text-slate-800">{user?.email}</p></div><div className="rounded-2xl border border-slate-100 bg-white/80 p-5"><ShieldCheck className="text-indigo-600" size={20} /><p className="mt-4 text-xs font-medium text-slate-500">Workspace role</p><p className="mt-1 font-semibold capitalize text-slate-800">{user?.role}</p></div><div className="rounded-2xl border border-slate-100 bg-white/80 p-5"><BadgeCheck className="text-emerald-600" size={20} /><p className="mt-4 text-xs font-medium text-slate-500">Account status</p><p className="mt-1 font-semibold text-emerald-700">Active</p></div></div></motion.section></main>; }
