import { motion } from "framer-motion";
import { Mail, ShieldCheck, UserRound } from "lucide-react";
import { useAuth } from "../context/AuthContext";

export default function ProfilePage() { const { user } = useAuth(); return <main className="p-6 sm:p-10"><motion.div initial={{opacity:0,y:12}} animate={{opacity:1,y:0}} className="max-w-3xl rounded-3xl border border-slate-200 bg-white p-8 shadow-xl"><div className="flex items-center gap-5"><div className="grid h-16 w-16 place-items-center rounded-2xl bg-gradient-to-br from-blue-500 to-indigo-600 text-white"><UserRound size={30}/></div><div><h1 className="text-3xl font-bold text-slate-900">{user?.name}</h1><p className="mt-1 text-slate-500">Your Ledger account profile</p></div></div><div className="mt-8 grid gap-4 sm:grid-cols-2"><div className="rounded-2xl bg-slate-50 p-5"><Mail className="text-blue-600"/><p className="mt-3 text-sm text-slate-500">Email</p><p className="font-semibold text-slate-900">{user?.email}</p></div><div className="rounded-2xl bg-slate-50 p-5"><ShieldCheck className="text-indigo-600"/><p className="mt-3 text-sm text-slate-500">Role</p><p className="font-semibold capitalize text-slate-900">{user?.role}</p></div></div></motion.div></main>; }
